for only experimenting with C++ language and to understand the concept of making a compiler.

Usage:

To compile and get the actual compiler, run:
1 - g++ main.cpp dist/main

To use the output compiler:

./dist/main <FILE> or simply run ./dist/main ./lang.txt